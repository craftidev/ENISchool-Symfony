{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(wishForm) }}
    {{ form_widget(wishForm) }}
    <button>Create</button>
    {{ form_end(wishForm) }}
{% endblock %}

{% block title %}
	{{parent()}}
	| Create
{% endblock %}
